import { User } from "lucide-react";
import { IconButton } from "../../ui/icon-button";
function UserButton() {
  return (
    <IconButton>
      <User />
    </IconButton>
  );
}

export default UserButton;